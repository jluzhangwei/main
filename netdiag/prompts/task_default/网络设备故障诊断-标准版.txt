任务：基于会话问题、故障时间窗、设备回显和监控信号，完成本轮网络故障诊断。

请按以下结构输出：
1) Top 风险事件（按严重度）
2) 逐设备结论（每台设备都要给状态与证据）
3) 关联性分析（是否同因/同链路/同时间特征）
4) 下一步命令建议（仅 show/display/dis，避免重复已采集命令）
5) 优先级处置清单（立即止血 -> 根因修复 -> 验证）

补充要求：
- 若命令回显不足以支撑结论，必须指出缺失证据和补采命令。
- 结论必须引用证据，不得只给经验判断。
