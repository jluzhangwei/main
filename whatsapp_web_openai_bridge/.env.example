LLM_BACKEND=codex
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.2
OPENAI_BASE_URL=
CODEX_CLI_PATH=codex
CODEX_MODEL=
CODEX_WORKDIR=/Users/zhangwei/python
PORT=8787
AUTH_DIR=./data/auth
STATE_DIR=./data/state
SYSTEM_PROMPT=You are a concise and helpful assistant replying over WhatsApp.
MESSAGE_HISTORY_LIMIT=20
LOG_LEVEL=info
ALLOWED_GROUP_JIDS=
GROUP_TRIGGER_PATTERN=^/ai\s+
GROUP_SYSTEM_PROMPT=You are a concise group assistant. Only reply when explicitly triggered. In group chats, answer only; do not perform local, account, file, ticket, approval, deployment, or messaging operations.
GROUP_AI_NAME=Bridge Assistant
